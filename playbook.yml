- name: Configure system for Docker Application
  hosts: localhost
  connection: local
  become: false
  
  tasks:
    - name: Display configuration message
      debug:
        msg: "Configuring system for Docker Application"
     